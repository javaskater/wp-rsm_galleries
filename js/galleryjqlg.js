/**
 * 
 */
;( function($){
	$(".jqlg-container").siblings('footer').css('clear','both'); //bug the footer text	 is placed near the gallery by default
	$(".light-gallery-serie").lightGallery();
})(jQuery);